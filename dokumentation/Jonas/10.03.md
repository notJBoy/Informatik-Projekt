# 10.03.2026

## Aufgabe des Tages

Der Nutzer hat verlangt:
> füge einen weiteren tab und dementsprechend widget für einen kalender hinzu. dort werden eingetragene klassenarbeiten und todos einegtragen und es können noch extra termine einegtragen werden

## Gemachte Änderungen

- **Neue Tab-Seite:** `tabs/calendar.php` erstellt.
  - Monatsübersicht mit Navigation (◀/▶).
  - Tagesansicht mit Liste aller Termine.
  - Formular zum Hinzufügen eigener Termine (Titel, Datum, Beschreibung).

- **Navigation & Inklusion:**
  - Neuer Navigationspunkt `📆 Kalender` in der Seitenleiste (`current_dashboard.php`).
  - Tab beim Laden des Dashboards eingebunden.
  - Übersicht-Widget „Kommende Termine“ im Dashboard (in `tabs/overview.php`).

- **Frontend-Logik (`current_dashboard.php`):**
  - Neue Variablen: `calendarExtras`, `currentCalMonth`, `currentCalYear`, `currentSelectedDate`.
  - Funktionen hinzugefügt/erweitert:
    - `saveCalendarExtras()`, `getAllCalendarItems()` (vereint eigene Einträge, Klassenarbeiten aus `exams`, ToDos).
    - `renderCalendar()`, `showEventsForDate()`, `prevMonth()`, `nextMonth()`, `deleteCalendarEvent()`
    - Integration in bestehende Funktionen (`addExam`, `deleteExam`, `loadTodos` usw.) zur Aktualisierung des Kalenders und des Übersichts-Widgets.
    - `renderOverviewCalendar()` für Übersichtsdashboard.
    - Initialisierung (`initCalendar()`) und Updates beim Tab-Wechsel.

- **CSS:** Zusätzliche Styles im `<style>`-Block von `current_dashboard.php` für Kalender-Zellen, Event-Dots, Farbcodierung der Ereignisse.

- **Datenhaltung:**
  - Eigene Termine werden in `localStorage` unter `calendar_extras` abgelegt.
  - Klassenarbeiten (`exams_data`) und ToDos (`todosData`) werden in die Monatsansicht übernommen.

- **Interaktion:**
  - Klick auf Widget- bzw. Sidebar-Buttons navigiert zum Kalender.
  - Ereignislisten zeigen Icons je Typ und bei Extras Lösch-Buttons.

- **Übersichts-Widget:** Zeigt die nächsten drei anstehenden Termine aller Typen.

Damit ist das Kalender-Feature vollständig eingebaut und interagiert mit bestehenden Modulen (Stundenplan, Prüfungen, ToDos). Es bleibt lokal gespeichert; serverseitige Erweiterung möglich.

---

## Zusätzliche Änderung – Hausaufgaben ausgelagert

Nutzerwunsch:
> lagere Hausaufgaben aus dem stundenplan tab in einen eigenen tab aus. lasse diese dann aber in dem stundenplan anzeigen

- **Neues Tab:** `tabs/homework.php` angelegt mit eigenem Widget für Hausaufgaben.
- **Sidebar:** Neuer Menüpunkt `📚 Hausaufgaben` ergänzt und Tab im Dashboard eingebunden.
- **Timetable-Tab:** Der ursprüngliche Hausaufgaben‑Widget wurde entfernt; stattdessen zeigt das Tab jetzt nur noch eine **Vorschau** (`#homeworkGridTimetable`) ohne Eingabefelder.
- **JS-Anpassungen:**
  - `renderHomework(editable, containerId)` ergänzt – erlaubt wahlweise nur Anzeige oder volle Eingabefunktion.
  - Beim Anlegen/Löschen von Hausaufgaben werden beide Container aktualisiert.
  - Anzeige in `timetable` erneuert, wenn der Tab geöffnet wird.
  - Initialisierung rendert sowohl die Vollansicht als auch die Vorschau.
- Die Hausaufgaben-Daten (`localStorage` `homework_data`) bleiben unverändert; Bearbeitung erfolgt fortan im eigenen Tab, Anzeige jedoch weiterhin im Stundenplan.

- **Zusätzliches Widget:** In der Übersichtsseite wurde ein neues Widget für anstehende Hausaufgaben eingefügt (`overviewHomeworks`), das maximal drei kommende Aufgaben mit Wochentagsangabe zeigt.
- **Stundenplan-Anzeige angepasst:** Innerhalb des Stundenplan-Grids werden Hausaufgaben für den jeweiligen Wochentag ebenfalls angezeigt – ähnlich wie Klausuren mit kleinen 📚‑Badges. Zellen mit Hausaufgaben erhalten die Klasse `.has-homework` und eine grüne Hervorhebung. Die Vorschau in `tabs/timetable.php` nutzt dafür `#homeworkGridTimetable`.


---

Speicherdatum: automatisch generiert durch Copilot.
