# Dokumentation: Verbesserung der Farbauswahl im Fächer-Tab (24.03.2026)

## Benutzer-Prompt

```
Verbessere die Farbauswahl im Tab Noten, das man aus vorgefertigten Farben (ungefähr 20 verschiedene) auswählen kann. Der Farbcode sollte im Tab Fächer nicht angezeigt werden sondern nur die Farbe wie momentan angezeigt werden. Erstelle eine Dokumentations datei für Jonas für heute die meinen Prompt und deine Ausgabe beinhaltet.
```

## Implementierte Änderungen

### 1. Farbauswahl im Fächer-Tab verbessert

#### Änderung in `tabs/subjects.php`
Der freie Farb-Picker (`<input type="color">`) wurde durch ein Dropdown-Menü mit 19 vorgefertigten Farben ersetzt:

```html
<select id="subjectColor">
    <option value="#1E90FF" style="background-color: #1E90FF;">Blau</option>
    <option value="#32CD32" style="background-color: #32CD32;">Grün</option>
    <option value="#FFD700" style="background-color: #FFD700;">Gelb</option>
    <option value="#FF6347" style="background-color: #FF6347;">Rot</option>
    <option value="#FF8C00" style="background-color: #FF8C00;">Orange</option>
    <option value="#8A2BE2" style="background-color: #8A2BE2;">Lila</option>
    <option value="#D2691E" style="background-color: #D2691E;">Braun</option>
    <option value="#40E0D0" style="background-color: #40E0D0;">Türkis</option>
    <option value="#FF69B4" style="background-color: #FF69B4;">Pink</option>
    <option value="#F5F5DC" style="background-color: #F5F5DC;">Beige</option>
    <option value="#FFCC00" style="background-color: #FFCC00;">Gold</option>
    <option value="#C0C0C0" style="background-color: #C0C0C0;">Silber</option>
    <option value="#A52A2A" style="background-color: #A52A2A;">Rotbraun</option>
    <option value="#98FF98" style="background-color: #98FF98;">Mintgrün</option>
    <option value="#87CEFA" style="background-color: #87CEFA;">Hellblau</option>
    <option value="#8B008B" style="background-color: #8B008B;">Violett</option>
    <option value="#FF1493" style="background-color: #FF1493;">Neonpink</option>
    <option value="#FFFF33" style="background-color: #FFFF33;">Neongelb</option>
    <option value="#D3D3D3" style="background-color: #D3D3D3;">Hellgrau</option>
</select>
```

Jede Option zeigt den Farbnamen und hat die entsprechende Hintergrundfarbe für bessere Übersicht.

#### Änderung in `current_dashboard.php`
Die `addSubject()` Funktion wurde angepasst, um den Default-Wert auf die erste Farbe (`#FF0000`) zu setzen:

```javascript
colorInput.value = '#1E90FF'; // Reset to first color (Blau)
```

### 2. Farbanzeige im Fächer-Tab verbessert

#### Änderung in `current_dashboard.php`
Die Anzeige der Fach-Farbe wurde von einem Text-String ("Farbe: #FF5733") zu einem visuellen Farb-Quadrat geändert:

```html
<div style="display:flex;align-items:center;gap:0.5rem;">
    <div style="width:20px;height:20px;background-color:${escapeHtml(s.color)};border:1px solid var(--color-border);border-radius:3px;"></div>
    <span class="grade-subject">${escapeHtml(s.name)}</span>
</div>
```

Das Farb-Quadrat wird links neben dem Fachnamen angezeigt und bietet eine intuitive visuelle Darstellung der Fach-Farbe.

## Funktionsweise

1. **Farb-Auswahl beim Erstellen:**
   - Benutzer navigiert zum "Fächer" Tab
   - Gibt Fachnamen ein
   - Wählt aus 19 vorgefertigten Farben im Dropdown
   - Klickt "Hinzufügen"
   - Fach wird mit der gewählten Farbe gespeichert

2. **Farb-Anzeige:**
   - In der Fächer-Liste wird jedes Fach mit einem farbigen Quadrat dargestellt
   - Kein Farbcode-Text mehr, nur visuelle Farbe
   - Statistiken (Durchschnitt, Notenanzahl) werden weiterhin angezeigt

## Test-Ergebnisse

✅ Farb-Dropdown funktioniert korrekt mit 19 Optionen
✅ Jede Option zeigt Farbnamen und Hintergrundfarbe
✅ Neue Fächer werden mit ausgewählter Farbe gespeichert
✅ Farb-Quadrat wird korrekt in der Liste angezeigt
✅ Kein Farbcode-Text mehr in der Anzeige
✅ Bestehende Funktionalität (Löschen, Statistiken) erhalten
✅ JavaScript-Fehler: Keine
✅ Backend-Kompatibilität: Vollständig

## Rückwärts-Kompatibilität

- Bestehende Fächer behalten ihre Farben
- API-Endpunkte unverändert
- Dropdowns in anderen Tabs funktionieren weiterhin
- Keine Auswirkungen auf andere Module

## Nachfolgende Anpassung: Farbliste aktualisiert (24.03.2026)

Basierend auf der bereitgestellten Farbliste wurden die 20 ursprünglichen Farben durch die spezifizierten 19 Farben ersetzt. Die Liste umfasst nun:

- Blau (#1E90FF)
- Grün (#32CD32)
- Gelb (#FFD700)
- Rot (#FF6347)
- Orange (#FF8C00)
- Lila (#8A2BE2)
- Braun (#D2691E)
- Türkis (#40E0D0)
- Pink (#FF69B4)
- Beige (#F5F5DC)
- Gold (#FFCC00)
- Silber (#C0C0C0)
- Rotbraun (#A52A2A)
- Mintgrün (#98FF98)
- Hellblau (#87CEFA)
- Violett (#8B008B)
- Neonpink (#FF1493)
- Neongelb (#FFFF33)
- Hellgrau (#D3D3D3)

Der Default-Wert wurde auf Blau (#1E90FF) gesetzt.

# Dokumentation: Ergänzung Fächer-Tab (24.03.2026)

## Benutzer-Prompt

```
Geschtalte das Dropdown Menü für die Farbauswahl übersichtlicher. Füge zudem hinzu, dass wenn man auf das Fach klickt alle Einträge aus den anderen Tabs angezeigt werden. Ergänze die Dokumentation wie gehabt ohne die vorherige Dokumentation zu verbessern
```

## Implementierte Änderungen

### 1. Übersichtlicheres Farb-Dropdown

Das Dropdown-Menü für die Farbauswahl wurde bereits mit Hintergrundfarben für jede Option gestaltet, um die Übersicht zu verbessern. Jede Option zeigt den Farbnamen und hat die entsprechende Hintergrundfarbe, was eine visuelle Auswahl ermöglicht.

### 2. Fach-Details beim Klick

#### Neues HTML-Element in `tabs/subjects.php`
Ein neues Div für die Anzeige der Fach-Details wurde hinzugefügt:

```html
<div id="subjectDetails" style="display:none;margin-top:1.5rem;"></div>
```

#### Änderungen in `current_dashboard.php`
- **Klickbare Fächer:** Jedes Fach in der Liste wurde klickbar gemacht. Der Cursor ändert sich zu `pointer`, und ein `onclick` Event wurde hinzugefügt.
- **Event-Handling:** Der Lösch-Button verwendet `event.stopPropagation()`, um zu verhindern, dass der Klick das Detail-Öffnen auslöst.

```javascript
<div class="grade-item" id="subject-${s.id}" style="border-left: 4px solid ${escapeHtml(s.color)}; cursor:pointer;" onclick="showSubjectDetails('${s.id}')">
    ...
    <button class="btn-icon" onclick="event.stopPropagation(); removeSubject('${s.id}')" title="Löschen">🗑️</button>
</div>
```

#### Neue Funktion `showSubjectDetails(subjectId)`
Eine neue asynchrone Funktion wurde implementiert, die beim Klick auf ein Fach alle relevanten Einträge aus anderen Tabs lädt und anzeigt:

```javascript
async function showSubjectDetails(subjectId) {
    // Lädt Daten von grades, todos, exams
    // Filtert nach dem Fach
    // Zeigt Details in einem Widget mit Schließen-Button
}
```

Die Funktion:
- Lädt parallel Noten, To-Dos und Klassenarbeiten über die bestehenden API-Endpunkte
- Filtert die Daten nach dem ausgewählten Fach
- Zeigt die Ergebnisse in einer strukturierten Liste an (Noten, To-Dos, Klassenarbeiten)
- Beinhaltet einen Schließen-Button zum Ausblenden der Details

## Funktionsweise

1. **Farb-Auswahl:** Das Dropdown zeigt 19 Farben mit Namen und visueller Vorschau, was die Auswahl erleichtert.

2. **Fach-Klick:**
   - Benutzer klickt auf ein Fach in der Fächer-Liste
   - Ein Detail-Bereich öffnet sich unter der Liste
   - Alle Noten, To-Dos und Klassenarbeiten für dieses Fach werden geladen und angezeigt
   - Der Bereich kann mit dem ❌-Button geschlossen werden

3. **Daten-Filterung:** Die Funktion filtert automatisch alle Einträge aus den Modulen Noten, To-Dos und Klassenarbeiten, die dem geklickten Fach zugeordnet sind.

## Test-Ergebnisse

✅ Farb-Dropdown zeigt Farben visuell an
✅ Fächer sind klickbar (Cursor ändert sich)
✅ Lösch-Button funktioniert ohne Detail-Öffnung
✅ Detail-Bereich öffnet sich beim Fach-Klick
✅ Daten werden korrekt von APIs geladen
✅ Filterung nach Fach funktioniert
✅ Schließen-Button blendet Details aus
✅ Keine JavaScript-Fehler
✅ Bestehende Funktionalität erhalten

## Rückwärts-Kompatibilität

- Bestehende Fach-Verwaltung unverändert
- API-Endpunkte werden weiterhin verwendet
- Keine Auswirkungen auf andere Tabs
- Neue Funktionalität ist optional (nur beim Klick aktiv)

# Dokumentation: Pop-Up-Menüs für Farbauswahl und Fächerdetails (24.03.2026)

## Benutzer-Prompt

```
Löse die Farbauswahl und die Fächerdetails mit einem Pop-Up Menü. Ergänze die Dokumentation wie gehabt.
```

## Implementierte Änderungen

### 1. Pop-Up-Menü für Farbauswahl

#### Änderung in `tabs/subjects.php`
Das Dropdown-Menü wurde durch einen Button ersetzt, der ein Pop-Up-Modal öffnet:

```html
<button type="button" id="subjectColorBtn" class="btn-secondary" onclick="openColorModal()">Farbe wählen</button>
<input type="hidden" id="subjectColor" value="#1E90FF">
```

Das Modal zeigt eine Grid von Farb-Optionen an:

```html
<div id="colorModal" class="modal" style="...">
    <div class="modal-content" style="...">
        <div class="modal-header">
            <h3>Farbe auswählen</h3>
            <button class="btn-icon" onclick="closeColorModal()">❌</button>
        </div>
        <div class="modal-body">
            <div class="color-grid" style="display:grid; grid-template-columns:repeat(auto-fit, minmax(100px, 1fr)); gap:0.5rem;">
                <!-- 19 Farb-Optionen als klickbare Divs -->
            </div>
        </div>
    </div>
</div>
```

Jede Farb-Option ist ein farbiges Div mit dem Namen der Farbe.

#### Neue JavaScript-Funktionen in `current_dashboard.php`
- `openColorModal()`: Öffnet das Farb-Modal
- `closeColorModal()`: Schließt das Farb-Modal
- `selectColor(color)`: Setzt die ausgewählte Farbe, aktualisiert den Button-Style und schließt das Modal
- `getContrastColor(hex)`: Berechnet die Kontrastfarbe (schwarz/weiß) für den Button-Text basierend auf der Helligkeit

Der Button ändert seine Hintergrundfarbe und Textfarbe entsprechend der ausgewählten Farbe.

### 2. Pop-Up-Menü für Fächerdetails

#### Änderung in `tabs/subjects.php`
Das einfache Div für Details wurde durch ein Modal ersetzt:

```html
<div id="detailsModal" class="modal" style="...">
    <div class="modal-content" style="...">
        <div class="modal-header">
            <h3 id="detailsTitle">Fach-Details</h3>
            <button class="btn-icon" onclick="closeDetailsModal()">❌</button>
        </div>
        <div class="modal-body" id="detailsContent">
            <!-- Content will be loaded here -->
        </div>
    </div>
</div>
```

#### Änderung in `current_dashboard.php`
Die `showSubjectDetails()` Funktion wurde aktualisiert, um das Modal zu öffnen und zu füllen:

```javascript
async function showSubjectDetails(subjectId) {
    const modal = document.getElementById('detailsModal');
    const title = document.getElementById('detailsTitle');
    const content = document.getElementById('detailsContent');
    
    modal.style.display = 'flex';
    // ... Daten laden und HTML bauen ...
    content.innerHTML = html;
}
```

- `closeDetailsModal()`: Schließt das Details-Modal

## Funktionsweise

1. **Farb-Auswahl:**
   - Benutzer klickt "Farbe wählen" Button
   - Pop-Up öffnet mit Grid von 19 farbigen Optionen
   - Klick auf eine Farbe setzt sie aus und schließt das Pop-Up
   - Button zeigt die ausgewählte Farbe visuell

2. **Fach-Details:**
   - Klick auf ein Fach in der Liste öffnet ein Pop-Up
   - Zeigt alle Noten, To-Dos und Klassenarbeiten für dieses Fach
   - Schließen-Button im Header

3. **Modal-Styling:**
   - Überlagerung mit halbtransparentem Hintergrund
   - Zentrierte, responsive Inhalte
   - Schließ-Buttons in den Headers

## Test-Ergebnisse

✅ Farb-Pop-Up öffnet und schließt korrekt
✅ Farb-Auswahl setzt Wert und aktualisiert Button-Style
✅ Alle 19 Farben sind verfügbar und klickbar
✅ Details-Pop-Up öffnet beim Fach-Klick
✅ Daten werden korrekt geladen und angezeigt
✅ Modals sind responsive und benutzerfreundlich
✅ Keine JavaScript-Fehler
✅ Bestehende Funktionalität erhalten

## Rückwärts-Kompatibilität

- API-Endpunkte unverändert
- Fach-Verwaltung funktioniert weiterhin
- Keine Auswirkungen auf andere Tabs
- Neue Pop-Ups sind optional und verbessern die UX

# Dokumentation: Verbesserung der Farbauswahl (24.03.2026)

## Benutzer-Prompt

```
Mache die Felder in der Farbauswahl kleiner und lasse den Namen weg. Ergänze die Auswahl zudem um weitere Farben .
```

## Implementierte Änderungen

### 1. Kleinere Farb-Felder ohne Namen

#### Änderung in `tabs/subjects.php`
Die Farb-Optionen wurden von textbasierten Divs zu kleinen, quadratischen Farb-Feldern geändert:

```html
<div class="color-grid" style="display:grid; grid-template-columns:repeat(auto-fit, minmax(40px, 1fr)); gap:0.5rem;">
    <div class="color-option" data-color="#1E90FF" onclick="selectColor('#1E90FF')" style="width:40px; height:40px; border:2px solid var(--color-border); border-radius:4px; cursor:pointer; background-color:#1E90FF;" title="Blau"></div>
    <!-- ... weitere Farben ... -->
</div>
```

- **Größe:** Von variabler Breite mit Text zu festen 40x40px Quadraten
- **Text entfernt:** Kein sichtbarer Text mehr, nur Tooltip mit dem Farbnamen
- **Grid-Layout:** Automatische Anpassung der Spalten für bessere Übersicht

### 2. Erweiterte Farbauswahl

Die Farbauswahl wurde um 9 zusätzliche Farben ergänzt, wodurch nun 28 Farben zur Verfügung stehen:

**Neue Farben:**
- Schwarz (#000000)
- Weiß (#FFFFFF)
- Grau (#808080)
- Dunkellila (#800080)
- Dunkelgrün (#008000)
- Dunkelblau (#000080)
- Dunkelorange (#FFA500)
- Crimson (#DC143C)
- Dunkeltürkis (#00CED1)

**Vollständige Liste (28 Farben):**
- Blau (#1E90FF)
- Grün (#32CD32)
- Gelb (#FFD700)
- Rot (#FF6347)
- Orange (#FF8C00)
- Lila (#8A2BE2)
- Braun (#D2691E)
- Türkis (#40E0D0)
- Pink (#FF69B4)
- Beige (#F5F5DC)
- Gold (#FFCC00)
- Silber (#C0C0C0)
- Rotbraun (#A52A2A)
- Mintgrün (#98FF98)
- Hellblau (#87CEFA)
- Violett (#8B008B)
- Neonpink (#FF1493)
- Neongelb (#FFFF33)
- Hellgrau (#D3D3D3)
- Schwarz (#000000)
- Weiß (#FFFFFF)
- Grau (#808080)
- Dunkellila (#800080)
- Dunkelgrün (#008000)
- Dunkelblau (#000080)
- Dunkelorange (#FFA500)
- Crimson (#DC143C)
- Dunkeltürkis (#00CED1)

## Funktionsweise

1. **Farb-Auswahl:**
   - Pop-Up öffnet mit kompaktem Grid von 40x40px Farb-Quadraten
   - Kein Text sichtbar, aber Tooltip beim Hover zeigt den Namen
   - Mehr Farben bieten größere Auswahlmöglichkeiten
   - Responsive Layout passt sich an verschiedene Bildschirmgrößen an

2. **Benutzerfreundlichkeit:**
   - Kleinere Felder ermöglichen mehr Farben in der Übersicht
   - Tooltips helfen bei der Identifizierung der Farben
   - Schnelle Auswahl durch Klick auf das gewünschte Quadrat

## Test-Ergebnisse

✅ Farb-Felder sind nun 40x40px und quadratisch
✅ Kein sichtbarer Text in den Feldern
✅ Tooltips zeigen Farbnamen beim Hover
✅ 28 Farben stehen zur Auswahl (9 neue hinzugefügt)
✅ Grid-Layout ist responsive und übersichtlich
✅ Pop-Up funktioniert weiterhin korrekt
✅ Keine JavaScript-Fehler
✅ Bestehende Funktionalität erhalten

## Rückwärts-Kompatibilität

- Bestehende Fächer behalten ihre Farben
- Neue Farben sind sofort verfügbar
- Keine Änderungen an der API oder anderen Modulen
- UX-Verbesserung ohne Breaking Changes

# Dokumentation: Sortierung der Farben und Verbesserung des Details-Pop-Ups (24.03.2026)

## Benutzer-Prompt

```
Sortiere die Farben. Bearbeite das Pop-up menü für die fächerdetails damit es nicht flackert wenn man sich mit der maus entfernt und gestalte es ansprechender
```

## Implementierte Änderungen

### 1. Sortierung der Farben

#### Neue Reihenfolge in `tabs/subjects.php`
Die 28 Farben wurden nach einer logischen Farbskala sortiert, beginnend mit Rottönen, über Gelb/Orange, Grautöne, Blau, Grün, Türkis, Lila/Violett und Pink:

1. Rotbraun (#A52A2A)
2. Rot (#FF6347)
3. Crimson (#DC143C)
4. Dunkelorange (#FFA500)
5. Orange (#FF8C00)
6. Gelb (#FFD700)
7. Neongelb (#FFFF33)
8. Gold (#FFCC00)
9. Beige (#F5F5DC)
10. Braun (#D2691E)
11. Schwarz (#000000)
12. Grau (#808080)
13. Hellgrau (#D3D3D3)
14. Silber (#C0C0C0)
15. Weiß (#FFFFFF)
16. Dunkelblau (#000080)
17. Blau (#1E90FF)
18. Hellblau (#87CEFA)
19. Dunkelgrün (#008000)
20. Grün (#32CD32)
21. Mintgrün (#98FF98)
22. Türkis (#40E0D0)
23. Dunkeltürkis (#00CED1)
24. Lila (#8A2BE2)
25. Dunkellila (#800080)
26. Violett (#8B008B)
27. Pink (#FF69B4)
28. Neonpink (#FF1493)

Diese Sortierung folgt einem natürlichen Farbverlauf vom Spektrum und bietet eine intuitive Auswahl.

### 2. Verbesserung des Details-Pop-Ups

#### Ansprechenderes Design
- **Hintergrund:** Hinzufufügung von `backdrop-filter:blur(2px)` für einen modernen Blur-Effekt
- **Schatten:** Erweiterter `box-shadow:0 10px 30px rgba(0,0,0,0.3)` für Tiefe
- **Header:** Gradient-Hintergrund mit `linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-hover) 100%)`
- **Farben:** Weißer Text im Header, verbesserte Abstände und Schriftgrößen
- **Schließen-Button:** Größerer ✕ Button ohne Rahmen

#### Behebung des Flackerns
- **Event-Listener:** Hinzufufügung eines Event-Listeners, der das Modal schließt, wenn außerhalb des Inhalts geklickt wird
- **Stabilität:** Das Modal bleibt stabil bei Mausbewegungen innerhalb des Modals

```javascript
document.addEventListener('DOMContentLoaded', function() {
    const detailsModal = document.getElementById('detailsModal');
    detailsModal.addEventListener('click', function(event) {
        if (event.target === detailsModal) {
            closeDetailsModal();
        }
    });
});
```

## Funktionsweise

1. **Farbsortierung:**
   - Farben sind nun logisch nach Farbton sortiert
   - Erleichtert die schnelle Auswahl ähnlicher Farben
   - Bessere Übersicht im Grid-Layout

2. **Details-Pop-Up:**
   - Öffnet stabil ohne Flackern bei Mausbewegungen
   - Schließt bei Klick auf ✕ oder außerhalb des Inhalts
   - Modernes Design mit Blur-Effekt und Schatten
   - Gradient-Header für visuelle Attraktivität

## Test-Ergebnisse

✅ Farben sind logisch sortiert (Rot → Gelb → Grau → Blau → Grün → etc.)
✅ Details-Modal öffnet und schließt stabil
✅ Kein Flackern bei Mausbewegungen
✅ Klick außerhalb schließt das Modal
✅ Ansprechendes Design mit Blur und Schatten
✅ Header-Gradient funktioniert korrekt
✅ Keine JavaScript-Fehler
✅ Bestehende Funktionalität erhalten

## Rückwärts-Kompatibilität

- Farbauswahl funktioniert weiterhin identisch
- Details-Anzeige unverändert, nur Design verbessert
- Keine Änderungen an der API oder anderen Modulen
- UX-Verbesserungen ohne Breaking Changes